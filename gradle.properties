pluginGroup=com.github.jhordyhuaman
pluginName=parquet-studio
pluginVersion=1.6.0
changeNotes=Fix: Windows long-path and special-character file loading (DuckDB pattern error). Rewritten file-opening flow (no more intermittent open/close failures). Warn on unsaved changes and on values converted to NULL during save. Removed save-time crash on files with 10 or fewer columns.

# See https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
# for insight into build numbers and IntelliJ Platform versions.
pluginSinceBuild=233

# IntelliJ Platform Properties -> https://github.com/JetBrains/gradle-intellij-plugin#intellij-platform-properties
platformType=IC
platformVersion=2023.3
javaVersion=17

gradleVersion=7.6.3

kotlin.stdlib.default.dependency=false
